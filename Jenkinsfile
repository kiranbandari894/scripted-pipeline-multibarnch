node{
  if(env.BRANCH_NAME == "master"){
     stage("BuldingMaster")"{
         echo "bulding master" 
     }
  }
  if(env.BRANCH_NAME == "dev"){
         stage("BuldingDEV")"{
         echo "bulding dev branch" 
     }
  }
}
