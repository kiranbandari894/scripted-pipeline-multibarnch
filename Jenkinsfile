node{
  if(env.BRANCH_NAME == "master"){
     stage("BuldingMaster"){
         echo "bulding ${env.BRANCH_NAME}" 
     }
  }
  if(env.BRANCH_NAME == "dev"){
         stage("BuldingDEV"){
         echo "bulding ${env.BRANCH_NAME}" 
     }
  }
}
